version: '3.8'

services:
  bot:
    image: ghcr.io/haykor/queue-bot:latest
    container_name: queue_bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
